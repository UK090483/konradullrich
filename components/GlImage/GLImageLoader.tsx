import * as React from "react";

interface IGLImageLoaderProps {}

const GLImageLoader: React.FunctionComponent<IGLImageLoaderProps> = (props) => {
  return <div></div>;
};

export default GLImageLoader;
